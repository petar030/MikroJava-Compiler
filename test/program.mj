program SomeTest
enum E {
    A = 1,
    B,
    C = 5
}
{
void main() int a[], m[], x, y, z; {
    x = a.length;          // OK: a je niz -> length: int

    y = E.A;               // OK: vrednost enum konstante (int)
    z = E.B;               // OK: podrazumevana vrednost (2 ako A=1)
    x = m.length;          // OK: m je niz nizova -> length: int
   
}
}
